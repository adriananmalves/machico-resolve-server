# machico-resolve-server

## How to run

- npm install on the console
- run Apache and mySql in XAMP
- Go to http://127.0.0.1/phpmyadmin/ and create a database called 'db'
- run node app.js on the console
